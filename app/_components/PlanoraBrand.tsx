export default function PlanoraBrand({ size = "text-3xl" }) {
  return (
    <div className={`font-light tracking-tight ${size} text-primary-50`}>
      <span className="font-semibold text-accent-500">Plan</span>
      ora
    </div>
  );
}
